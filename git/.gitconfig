[user]
	name = robertycxu
	email = robertycxu@fb.com

[core]
	editor = vim

[alias]
	co = checkout
	st = status
[credential]
	helper =
	helper = /usr/local/share/gcm-core/git-credential-manager-core
[credential "https://dev.azure.com"]
	useHttpPath = true
[pager]
	diff = false
[color]
	ui = auto
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
; [http]
;     proxy = https://fwdproxy:8082
;     proxySSLCert=/var/facebook/credentials/<login_user>/x509/<real_user>.pem
;     proxySSLKey=/var/facebook/credentials/<login_user>/x509/<real_user>.pem
